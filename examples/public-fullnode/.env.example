# AWS Configuration
AWS_PROFILE=your-aws-profile
AWS_REGION=us-east-1

# Cluster Configuration
VALIDATOR_NAME=demo

# VPC Configuration (optional)
# VPC_CIDR=10.0.0.0/20

# DNS Configuration (optional)
ENABLE_DNS=false
# DNS_ZONE_NAME=example.com
# FULLNODE_DNS_NAME=fullnode.example.com

# Bootstrap Configuration
BOOTSTRAP_S3_BUCKET=your-backup-bucket
BOOTSTRAP_S3_PREFIX=path/to/db
BOOTSTRAP_S3_REGION=us-west-2

# Node Configuration (optional)
# NODE_INSTANCE_TYPES=m5.2xlarge,m6i.2xlarge,c5.2xlarge

# Fullnode Configuration (optional)
# FULLNODE_NAMESPACE=movement-l1
# FULLNODE_SERVICE_NAME=public-fullnode
# FULLNODE_CONFIG_FILE=configs/testnet.pfn-restore.yaml