apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "movement-node.fullname" . }}-config
  labels:
{{ include "movement-node.labels" . | indent 4 }}
data:
  node.yaml: |-
{{ if .Values.config.inline }}
{{ tpl .Values.config.inline . | indent 4 }}
{{ else }}
{{ tpl (.Files.Get "files/fullnode.yaml") . | indent 4 }}
{{ end }}
